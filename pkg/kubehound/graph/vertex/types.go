package vertex

var (
	// Labels is a list of all possible labels for a vertex in the graph.
	Labels = []string{
		ContainerLabel,
		EndpointLabel,
		IdentityLabel,
		NodeLabel,
		PermissionSetLabel,
		PodLabel,
		VolumeLabel,
	}
)
